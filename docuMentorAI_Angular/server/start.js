/*
const app = require('./server'); // adjust the path if you have moved the express setup to app.js
const port = process.env.PORT || 3000;

app.listen(port, () => {
    console.log(`Server running on port ${port}`);
});
*/